# ===========================================
# FICHIER EXEMPLE - À COPIER EN .env.local
# ===========================================
# NE JAMAIS COMMIT LE FICHIER .env.local !

# ===========================================
# DATABASE
# ===========================================
DATABASE_URL="postgresql://admin:MOT_DE_PASSE_SECURISE@db:5432/formation_db"

# ===========================================
# LDAP / ACTIVE DIRECTORY
# ===========================================
# Demander ces informations à l'équipe AD
LDAP_URL="ldap://IP_SERVEUR_AD:389"
LDAP_BASE_DN="dc=dingolingo,dc=local"
LDAP_BIND_DN="cn=admin,dc=dingolingo,dc=local"
LDAP_BIND_PASSWORD="MOT_DE_PASSE_AD"

# ===========================================
# MODE AUTHENTIFICATION
# ===========================================
# true  = Mode test (admin/admin ou user/test)
# false = Mode production (connexion AD réelle)
NEXT_PUBLIC_MOCK_AUTH="false"

# ===========================================
# TRAEFIK - REVERSE PROXY (PRODUCTION)
# ===========================================
# Nom de domaine acheté
DOMAIN=dingolingo.be

# Email pour Let's Encrypt (notifications certificat)
ACME_EMAIL=admin@dingolingo.be

# Auth dashboard Traefik (optionnel)
# Générer avec: docker run --rm httpd:2.4-alpine htpasswd -nb admin MonMotDePasse
# Doubler les $ dans la valeur
# TRAEFIK_AUTH=admin:$$apr1$$xyz$$hashedpassword
